version: "2"
linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - bodyclose
    - errorlint
    - revive
  settings:
    revive:
      rules:
        - name: exported
formatters:
  enable:
    - gofmt
